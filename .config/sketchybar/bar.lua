local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  topmost = "window",
  height = 40,
  color = "NONE",
  padding_right = 1,
  padding_left = 1,
  y_offset = 6,
  margin = 14,
  corner_radius = 11
})
